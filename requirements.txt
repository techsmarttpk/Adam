# Core configuration (Milestone 4 -- adam.common.config)
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# Collectors (Phase 7 -- adam.collectors)
python-evtx>=0.8.1,<0.9

# Orchestrator & CLI (Phase 8 -- adam.orchestrator, adam.cli)
rich>=13,<15
typer>=0.12,<1

# HTTP Guest Agent channel (Phase 5 -- adam.sandbox.guest.http_channel)
httpx>=0.27,<1

# Policy/Deception rule loading (nived-dev merge -- adam.policy)
PyYAML>=6.0

# NOTE: pytest and pytest-asyncio are intentionally NOT listed here.
# They are dev-only (ARCHITECTURE.md section 15.3's "Dev-only" row) and
# already declared, with tighter version pins, in requirements-dev.txt.
