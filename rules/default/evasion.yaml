- id: RULE-022
  version: "1.0.0"
  description: >
    Sandbox evasion detected
  when:
    intent: EVADE_SANDBOX_DETECTED
    confidence_gte: 0.0
  then:
    action: LOG_ONLY
    priority: 90
  budget:
    max_per_session: 1
    cooldown_seconds: 30
